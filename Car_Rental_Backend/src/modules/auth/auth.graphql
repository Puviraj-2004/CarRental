type LoginPayload {
  accessToken: String!
  refreshToken: String! 
  user: User!
}

type RefreshTokensPayload {
  accessToken: String!
  refreshToken: String! 
}

extend type Mutation {
  register(input: RegisterInput!): RegisterPayload!
  verifyOTP(email: String!, otp: String!): VerifyOTPPayload!
  resendOTP(email: String!): ResendOTPPayload!
  login(input: LoginInput!): LoginPayload!
  refreshTokens(refreshToken: String!): RefreshTokensPayload! 
  logout(refreshToken: String!): Boolean                    
}

input RegisterInput {
  email: String!
  password: String!
  phoneNumber: String
}

input LoginInput {
  email: String!
  password: String!
}

type RegisterPayload {
  message: String!
  email: String!
}

type VerifyOTPPayload {
  success: Boolean!
  message: String!
}

type ResendOTPPayload {
  success: Boolean!
  message: String!
  expiresAt: DateTime!
}